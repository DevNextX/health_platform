apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: health-platform
  labels:
    app: frontend
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
