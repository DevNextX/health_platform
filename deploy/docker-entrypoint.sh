#!/bin/sh
# Generated by Zhuang
set -e

# Ensure env vars substituted into nginx template
if [ -f /etc/nginx/templates/default.conf.template ]; then
  envsubst '${API_URL} ${LISTEN_PORT}' < /etc/nginx/templates/default.conf.template > /etc/nginx/conf.d/default.conf
fi

exec "$@"
