# Generated by Zhuang; Generated by Copilot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: health-platform
spec:
  template:
    spec:
      securityContext:
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        runAsNonRoot: true
      volumes:
        - name: nginx-conf
          configMap:
            name: frontend-nginx-conf
            items:
              - key: nginx-default.conf
                path: default.conf
      containers:
        - name: frontend
          # Override ENTRYPOINT to skip /docker-entrypoint.sh which tries to write conf
          command: ["nginx"]
          args: ["-g", "daemon off;"]
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d
              readOnly: true