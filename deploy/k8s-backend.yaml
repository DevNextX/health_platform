# Generated by Zhuang
apiVersion: v1
kind: Namespace
metadata:
  name: health-platform
---
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: health-platform
stringData:
  JWT_SECRET: "change-me"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: health-platform
data:
  PORT: "5000"
  CORS_ORIGINS: "http://localhost:8080,http://127.0.0.1:8080"
  JWT_ACCESS_MINUTES: "30"
  JWT_REFRESH_DAYS: "7"
  # DB auto-create controls (see README)
  DB_CREATE_ON_MISSING: "1"
  DB_AUTO_CREATE: "0"
---
apiVersion: v1
kind: Secret
metadata:
  name: db-conn
  namespace: health-platform
stringData:
  # Replace with your external DB connection string securely
  # Example: mysql+pymysql://user:password@mysql:3306/healthdb?charset=utf8mb4
  DATABASE_URL: "mysql+pymysql://user:password@mysql:3306/healthdb?charset=utf8mb4"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: health-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        runAsNonRoot: true
      containers:
        - name: backend
          image: ghcr.io/devnextx/health-platform-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secrets
            - secretRef:
                name: db-conn
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: health-platform
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
