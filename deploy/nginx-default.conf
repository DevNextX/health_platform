
# Generated by Zhuang; Generated by Copilot
server {
  listen 8080;

  # Proxy API to backend service (in same namespace)
  location /api/ {
    proxy_pass http://backend-svc.health-platform.svc.cluster.local:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
  }

  # Serve static files and fallback to SPA index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: basic static asset caching
  location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
