"""
Health manager layer. Generated by Zhuang
"""
import json
from datetime import datetime, timedelta
from typing import List, Optional, Tuple, Dict
from sqlalchemy import or_, select, and_
from ..extensions import db
from ..models import HealthRecord, RecordSubject
from ..resilience.policy import db_breaker, with_retry


def truncate_to_minute(dt: datetime) -> datetime:
    """Truncate datetime to minute precision (set seconds and microseconds to 0)"""
    return dt.replace(second=0, microsecond=0)


class HealthManager:
    @db_breaker
    def check_duplicate(self, member_id: int, timestamp: datetime) -> bool:
        """
        Check if a health record exists for the given member at the same minute.
        Returns True if duplicate exists, False otherwise.
        """
        truncated_ts = truncate_to_minute(timestamp)
        start_of_minute = truncated_ts
        end_of_minute = truncated_ts + timedelta(seconds=59, microseconds=999999)
        
        # Query RecordSubject to get all record_ids for this member
        record_ids_subq = select(RecordSubject.record_id).where(RecordSubject.member_id == member_id).scalar_subquery()
        
        # Check if any HealthRecord exists in the same minute
        exists = db.session.query(HealthRecord.id)\
            .filter(HealthRecord.id.in_(record_ids_subq))\
            .filter(and_(
                HealthRecord.timestamp >= start_of_minute,
                HealthRecord.timestamp <= end_of_minute
            ))\
            .first()
        
        return exists is not None
    
    @db_breaker
    @with_retry()
    def create(self, user_id: int, systolic: int, diastolic: int, heart_rate: Optional[int],
               timestamp: datetime, tags: List[str], note: Optional[str]) -> HealthRecord:
        rec = HealthRecord()
        rec.user_id = user_id
        rec.systolic = systolic
        rec.diastolic = diastolic
        rec.heart_rate = heart_rate
        rec.timestamp = timestamp
        # Generated by Zhuang: preserve non-ASCII characters for exact contains on Chinese tags
        rec.tags = json.dumps(tags, ensure_ascii=False)
        rec.note = note
        
        db.session.add(rec)
        db.session.commit()
        return rec

    def _base_query(self, user_id: int):
        return HealthRecord.query.filter_by(user_id=user_id)

    @db_breaker
    def get(self, user_id: int, rec_id: int) -> Optional[HealthRecord]:
        return HealthRecord.query.filter_by(id=rec_id, user_id=user_id).first()

    @db_breaker
    def count(self, user_id: int, q):
        return q.count()

    @db_breaker
    def list(self, user_id: int, page: int, size: int, tags: Optional[List[str]],
             date_from: Optional[datetime], date_to: Optional[datetime], subject_member_id: Optional[int] = None) -> Tuple[int, List[HealthRecord]]:
        q = self._base_query(user_id)
        if subject_member_id is not None:
            subq = select(RecordSubject.record_id).where(RecordSubject.member_id == subject_member_id)
            q = q.filter(HealthRecord.id.in_(subq))
        if tags:
            # Generated by Zhuang: Use OR semantics and exact tag match; support both raw and \u-escaped storage
            # per_tag_condition = OR(contains('"餐后"'), contains('"\u9910\u540e"'))
            conditions = []
            for t in tags:
                raw = f'"{t}"'
                escaped = json.dumps(t, ensure_ascii=True)  # includes surrounding quotes
                conditions.append(or_(HealthRecord.tags.contains(raw), HealthRecord.tags.contains(escaped)))
            if conditions:
                q = q.filter(or_(*conditions))
        if date_from:
            q = q.filter(HealthRecord.timestamp >= date_from)
        if date_to:
            q = q.filter(HealthRecord.timestamp <= date_to)
        total = q.count()
        items = q.order_by(HealthRecord.timestamp.desc()).offset((page - 1) * size).limit(size).all()
        return total, items

    @db_breaker
    def list_all(self, user_id: int, tags: Optional[List[str]],
                 date_from: Optional[datetime], date_to: Optional[datetime], subject_member_id: Optional[int] = None) -> List[HealthRecord]:
        """Return all records matching the filters without pagination. Generated by Zhuang"""
        q = self._base_query(user_id)
        if subject_member_id is not None:
            subq = select(RecordSubject.record_id).where(RecordSubject.member_id == subject_member_id)
            q = q.filter(HealthRecord.id.in_(subq))
        if tags:
            # Generated by Zhuang: OR semantics; support raw and escaped forms
            conditions = []
            for t in tags:
                raw = f'"{t}"'
                escaped = json.dumps(t, ensure_ascii=True)
                conditions.append(or_(HealthRecord.tags.contains(raw), HealthRecord.tags.contains(escaped)))
            if conditions:
                q = q.filter(or_(*conditions))
        if date_from:
            q = q.filter(HealthRecord.timestamp >= date_from)
        if date_to:
            q = q.filter(HealthRecord.timestamp <= date_to)
        return q.order_by(HealthRecord.timestamp.asc()).all()

    @db_breaker
    @with_retry()
    def update(self, rec: HealthRecord, **fields) -> HealthRecord:
        for k, v in fields.items():
            if hasattr(rec, k):
                setattr(rec, k, v)
        
        db.session.commit()
        return rec

    @db_breaker
    @with_retry()
    def delete(self, rec: HealthRecord):
        db.session.delete(rec)
        db.session.commit()

    @db_breaker
    @with_retry()
    def bulk_create(self, records: List[dict]) -> Dict:
        """
        Batch insert health records with duplicate detection.
        Each record dict should contain: user_id, systolic, diastolic, heart_rate, timestamp, tags, note, member_id
        Returns dict with:
        - 'created': list of created HealthRecord objects
        - 'skipped': list of dicts with skipped record info and reason
        - 'success_count': int
        - 'skipped_count': int
        """
        objects = []
        skipped = []
        
        for idx, rec_data in enumerate(records):
            member_id = rec_data.get('member_id')
            timestamp = rec_data['timestamp']
            
            # Check for duplicate
            if member_id and self.check_duplicate(member_id, timestamp):
                skipped.append({
                    'index': idx,
                    'member_name': rec_data.get('member_name', 'Unknown'),
                    'timestamp': timestamp.strftime('%Y-%m-%d %H:%M'),
                    'reason': '该时间点已有记录'
                })
                continue
            
            rec = HealthRecord()
            rec.user_id = rec_data['user_id']
            rec.systolic = rec_data['systolic']
            rec.diastolic = rec_data['diastolic']
            rec.heart_rate = rec_data.get('heart_rate')
            rec.timestamp = timestamp
            rec.tags = json.dumps(rec_data.get('tags', []), ensure_ascii=False)
            rec.note = rec_data.get('note')
            objects.append(rec)
        
        db.session.bulk_save_objects(objects, return_defaults=True)
        db.session.commit()
        
        return {
            'created': objects,
            'skipped': skipped,
            'success_count': len(objects),
            'skipped_count': len(skipped)
        }
