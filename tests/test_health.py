"""
Test cases for health record endpoints. Generated by Zhuang
"""
import json
import pytest
from datetime import datetime


class TestHealthEndpoints:
    """Test health record related endpoints"""
    
    def test_create_health_record(self, client, auth_headers):
        """Test creating a new health record"""
        access_headers = auth_headers['access']
        
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72,
            'timestamp': '2025-08-22T10:00:00Z',
            'tags': ['morning', 'resting'],
            'note': 'Morning measurement'
        }
        
        response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        assert response.status_code == 201
        
        data = response.get_json()
        assert data['systolic'] == 120
        assert data['diastolic'] == 80
        assert data['heart_rate'] == 72
        assert data['tags'] == ['morning', 'resting']
        assert data['note'] == 'Morning measurement'
        assert 'id' in data
        assert 'created_at' in data
    
    def test_create_health_record_unauthorized(self, client):
        """Test creating health record without authorization"""
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72
        }
        
        response = client.post('/api/v1/health', json=record_data)
        assert response.status_code == 401
    
    def test_create_health_record_missing_fields(self, client, auth_headers):
        """Test creating health record with missing required fields"""
        access_headers = auth_headers['access']
        
        record_data = {
            'systolic': 120
            # Missing diastolic and heart_rate
        }
        
        response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        assert response.status_code == 400
    
    def test_list_health_records(self, client, auth_headers):
        """Test listing health records"""
        access_headers = auth_headers['access']
        
        # Create a few records first
        records = [
            {'systolic': 120, 'diastolic': 80, 'heart_rate': 72, 'tags': ['morning']},
            {'systolic': 130, 'diastolic': 85, 'heart_rate': 75, 'tags': ['evening']},
            {'systolic': 125, 'diastolic': 82, 'heart_rate': 70, 'tags': ['afternoon']}
        ]
        
        for record in records:
            client.post('/api/v1/health', json=record, headers=access_headers)
        
        # Test listing all records
        response = client.get('/api/v1/health', headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert 'records' in data
        assert 'pagination' in data
        assert len(data['records']) == 3
    
    def test_list_health_records_pagination(self, client, auth_headers):
        """Test health records pagination"""
        access_headers = auth_headers['access']
        
        # Create multiple records
        for i in range(25):
            record = {
                'systolic': 120 + i,
                'diastolic': 80,
                'heart_rate': 72,
                'tags': [f'test{i}']
            }
            client.post('/api/v1/health', json=record, headers=access_headers)
        
        # Test first page
        response = client.get('/api/v1/health?page=1&size=10', headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert len(data['records']) == 10
        assert data['pagination']['page'] == 1
        assert data['pagination']['size'] == 10
        assert data['pagination']['total'] == 25
        assert data['pagination']['pages'] == 3
        
        # Test second page
        response = client.get('/api/v1/health?page=2&size=10', headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert len(data['records']) == 10
        assert data['pagination']['page'] == 2
    
    def test_list_health_records_filter_by_tags(self, client, auth_headers):
        """Test filtering health records by tags"""
        access_headers = auth_headers['access']
        
        # Create records with different tags
        records = [
            {'systolic': 120, 'diastolic': 80, 'heart_rate': 72, 'tags': ['morning', 'home']},
            {'systolic': 130, 'diastolic': 85, 'heart_rate': 75, 'tags': ['evening', 'work']},
            {'systolic': 125, 'diastolic': 82, 'heart_rate': 70, 'tags': ['morning', 'gym']}
        ]
        
        for record in records:
            client.post('/api/v1/health', json=record, headers=access_headers)
        
        # Filter by 'morning' tag
        response = client.get('/api/v1/health?tags=morning', headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert len(data['records']) == 2
        for record in data['records']:
            assert 'morning' in record['tags']

        # Generated by Zhuang: Filter by multiple tags should use OR semantics
        response = client.get('/api/v1/health?tags=morning,work', headers=access_headers)
        assert response.status_code == 200
        data = response.get_json()
        # morning matches 2, work matches 1, but one record may overlap; in our dataset it's 3 total
        assert len(data['records']) == 3
        # Ensure each record has at least one of the requested tags
        for record in data['records']:
            assert any(t in record['tags'] for t in ['morning', 'work'])
    
    def test_get_health_record(self, client, auth_headers):
        """Test getting a specific health record"""
        access_headers = auth_headers['access']
        
        # Create a record first
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72,
            'note': 'Test record'
        }
        
        create_response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        record_id = create_response.get_json()['id']
        
        # Get the specific record
        response = client.get(f'/api/v1/health/{record_id}', headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert data['id'] == record_id
        assert data['systolic'] == 120
        assert data['note'] == 'Test record'
    
    def test_get_health_record_not_found(self, client, auth_headers):
        """Test getting non-existent health record"""
        access_headers = auth_headers['access']
        
        response = client.get('/api/v1/health/999', headers=access_headers)
        assert response.status_code == 404
    
    def test_update_health_record(self, client, auth_headers):
        """Test updating a health record"""
        access_headers = auth_headers['access']
        
        # Create a record first
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72,
            'note': 'Original note'
        }
        
        create_response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        record_id = create_response.get_json()['id']
        
        # Update the record
        update_data = {
            'systolic': 125,
            'note': 'Updated note'
        }
        
        response = client.put(f'/api/v1/health/{record_id}', json=update_data, headers=access_headers)
        assert response.status_code == 200
        
        data = response.get_json()
        assert data['systolic'] == 125
        assert data['diastolic'] == 80  # Should remain unchanged
        assert data['note'] == 'Updated note'
    
    def test_delete_health_record(self, client, auth_headers):
        """Test deleting a health record"""
        access_headers = auth_headers['access']
        
        # Create a record first
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72
        }
        
        create_response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        record_id = create_response.get_json()['id']
        
        # Delete the record
        response = client.delete(f'/api/v1/health/{record_id}', headers=access_headers)
        assert response.status_code == 200
        
        # Verify it's deleted
        get_response = client.get(f'/api/v1/health/{record_id}', headers=access_headers)
        assert get_response.status_code == 404
    
    def test_health_record_ownership(self, client, auth_headers):
        """Test that users can only access their own health records"""
        access_headers = auth_headers['access']
        
        # Create a record with first user
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72
        }
        
        create_response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        record_id = create_response.get_json()['id']
        
        # Create a second user
        client.post('/api/v1/auth/register', json={
            'username': 'user2',
            'email': 'user2@example.com',
            'password': 'password123'
        })
        
        # Login as second user
        login_response = client.post('/api/v1/auth/login', json={
            'email': 'user2@example.com',
            'password': 'password123'
        })
        
        user2_token = login_response.get_json()['access_token']
        user2_headers = {'Authorization': f'Bearer {user2_token}'}
        
        # Second user should not be able to access first user's record
        response = client.get(f'/api/v1/health/{record_id}', headers=user2_headers)
        assert response.status_code == 404  # Or 403, depending on implementation
    
    def test_blood_pressure_validation_updated_ranges(self, client, auth_headers):
        """Test updated blood pressure validation ranges (30-250 mmHg). Generated by Copilot."""
        access_headers = auth_headers['access']
        
        # Test valid values at new lower bound (30)
        valid_data = {
            'systolic': 120,
            'diastolic': 30,  # New minimum
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=valid_data, headers=access_headers)
        assert response.status_code == 201
        
        # Test valid values at upper bound (250)
        valid_data_high = {
            'systolic': 250,  # Maximum
            'diastolic': 180,
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=valid_data_high, headers=access_headers)
        assert response.status_code == 201
        
        # Test below minimum (29)
        invalid_low = {
            'systolic': 120,
            'diastolic': 29,  # Below minimum
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=invalid_low, headers=access_headers)
        assert response.status_code == 422
        assert "30-250 mmHg" in response.get_json()['message']
        
        # Test above maximum (251)
        invalid_high = {
            'systolic': 251,  # Above maximum
            'diastolic': 80,
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=invalid_high, headers=access_headers)
        assert response.status_code == 422
        assert "30-250 mmHg" in response.get_json()['message']
    
    def test_systolic_greater_than_diastolic_validation(self, client, auth_headers):
        """Test new requirement: systolic must be greater than diastolic. Generated by Copilot."""
        access_headers = auth_headers['access']
        
        # Test systolic equal to diastolic (should fail)
        invalid_equal = {
            'systolic': 120,
            'diastolic': 120,  # Equal to systolic
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=invalid_equal, headers=access_headers)
        assert response.status_code == 422
        assert "greater than diastolic" in response.get_json()['message']
        
        # Test systolic less than diastolic (should fail)
        invalid_less = {
            'systolic': 80,
            'diastolic': 120,  # Greater than systolic
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=invalid_less, headers=access_headers)
        assert response.status_code == 422
        assert "greater than diastolic" in response.get_json()['message']
        
        # Test valid case (systolic > diastolic)
        valid_data = {
            'systolic': 120,
            'diastolic': 80,  # Less than systolic
            'heart_rate': 72
        }
        response = client.post('/api/v1/health', json=valid_data, headers=access_headers)
        assert response.status_code == 201
    
    def test_heart_rate_validation_updated_ranges(self, client, auth_headers):
        """Test updated heart rate validation ranges (30-150 bpm). Generated by Copilot."""
        access_headers = auth_headers['access']
        
        # Test valid values at bounds
        valid_low = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 30  # New minimum
        }
        response = client.post('/api/v1/health', json=valid_low, headers=access_headers)
        assert response.status_code == 201
        
        valid_high = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 150  # New maximum
        }
        response = client.post('/api/v1/health', json=valid_high, headers=access_headers)
        assert response.status_code == 201
        
        # Test below minimum (29)
        invalid_low = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 29  # Below minimum
        }
        response = client.post('/api/v1/health', json=invalid_low, headers=access_headers)
        assert response.status_code == 422
        assert "30-150 bpm" in response.get_json()['message']
        
        # Test above maximum (151)
        invalid_high = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 151  # Above maximum
        }
        response = client.post('/api/v1/health', json=invalid_high, headers=access_headers)
        assert response.status_code == 422
        assert "30-150 bpm" in response.get_json()['message']
        
        # Test heart rate optional (None/empty should be valid)
        valid_no_hr = {
            'systolic': 120,
            'diastolic': 80
            # No heart_rate field
        }
        response = client.post('/api/v1/health', json=valid_no_hr, headers=access_headers)
        assert response.status_code == 201
        
        # Test heart rate empty string (should be treated as None)
        valid_empty_hr = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': ""  # Empty string
        }
        response = client.post('/api/v1/health', json=valid_empty_hr, headers=access_headers)
        assert response.status_code == 201
    
    def test_update_record_validation(self, client, auth_headers):
        """Test validation on record updates with new rules. Generated by Copilot."""
        access_headers = auth_headers['access']
        
        # Create a valid record first
        record_data = {
            'systolic': 120,
            'diastolic': 80,
            'heart_rate': 72
        }
        create_response = client.post('/api/v1/health', json=record_data, headers=access_headers)
        record_id = create_response.get_json()['id']
        
        # Test updating with invalid systolic > diastolic relationship
        invalid_update = {
            'systolic': 70,  # Less than existing diastolic (80)
        }
        response = client.put(f'/api/v1/health/{record_id}', json=invalid_update, headers=access_headers)
        assert response.status_code == 422
        assert "greater than diastolic" in response.get_json()['message']
        
        # Test updating diastolic to be >= systolic
        invalid_update2 = {
            'diastolic': 120,  # Equal to existing systolic (120)
        }
        response = client.put(f'/api/v1/health/{record_id}', json=invalid_update2, headers=access_headers)
        assert response.status_code == 422
        assert "greater than diastolic" in response.get_json()['message']
        
        # Test valid update with new heart rate range
        valid_update = {
            'heart_rate': 150  # At new maximum
        }
        response = client.put(f'/api/v1/health/{record_id}', json=valid_update, headers=access_headers)
        assert response.status_code == 200
        
        # Test invalid heart rate update
        invalid_hr_update = {
            'heart_rate': 151  # Above new maximum
        }
        response = client.put(f'/api/v1/health/{record_id}', json=invalid_hr_update, headers=access_headers)
        assert response.status_code == 422
        assert "30-150 bpm" in response.get_json()['message']
